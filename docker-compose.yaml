services:
  server:
    build: ./server
    container_name: server
    ports:
      - "5000:5000"
    networks:
      - net

  client:
    build: ./client
    container_name: client
    depends_on:
      - server
    networks:
      - net

networks:
  net:
    driver: bridge